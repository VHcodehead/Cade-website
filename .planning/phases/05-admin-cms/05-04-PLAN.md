---
phase: 05-admin-cms
plan: 04
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/app/actions/settings.ts
  - src/app/actions/logos.ts
  - src/app/(admin)/admin/settings/page.tsx
  - src/app/(admin)/admin/logos/page.tsx
  - src/components/admin/logo-grid.tsx
autonomous: true
requirements: [ADMIN-08, ADMIN-09, ADMIN-10, ADMIN-11]

must_haves:
  truths:
    - "Admin can update hero Vimeo URL, about text, contact email, location, Instagram URL, and Vimeo profile URL from a single settings form"
    - "Admin can upload a brand logo image file and see it appear in the grid"
    - "Admin can delete a brand logo from the grid"
    - "Settings changes reflect on the public site after save"
  artifacts:
    - path: "src/app/actions/settings.ts"
      provides: "updateSettings Server Action"
      exports: ["updateSettings"]
    - path: "src/app/actions/logos.ts"
      provides: "uploadLogo and deleteLogo Server Actions"
      exports: ["uploadLogo", "deleteLogo"]
    - path: "src/app/(admin)/admin/settings/page.tsx"
      provides: "Settings form page"
      min_lines: 30
    - path: "src/app/(admin)/admin/logos/page.tsx"
      provides: "Logo management page"
      min_lines: 15
    - path: "src/components/admin/logo-grid.tsx"
      provides: "Logo grid with upload form and delete"
      min_lines: 40
  key_links:
    - from: "src/app/(admin)/admin/settings/page.tsx"
      to: "src/app/actions/settings.ts"
      via: "form action"
      pattern: "updateSettings"
    - from: "src/components/admin/logo-grid.tsx"
      to: "src/app/actions/logos.ts"
      via: "uploadLogo and deleteLogo"
      pattern: "uploadLogo|deleteLogo"
---

<objective>
Build the settings page (hero video, about text, contact info) and brand logo management (upload, view grid, delete).

Purpose: Enables the owner to update site-wide content and manage brand logos without code changes.
Output: Working settings form at /admin/settings and logo manager at /admin/logos.
</objective>

<execution_context>
@C:/Users/Max/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Max/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-admin-cms/05-CONTEXT.md
@.planning/phases/05-admin-cms/05-RESEARCH.md
@.planning/phases/05-admin-cms/05-01-SUMMARY.md

@src/app/actions/contact.ts
@src/lib/dal.ts
@src/lib/db.ts
@prisma/schema.prisma

<interfaces>
<!-- SiteConfig model (after Plan 01 migration) -->
```
id: "singleton"
heroVimeoId, aboutText, contactEmail, location, instagramUrl, vimeoProfileUrl
```

<!-- Established Server Action pattern -->
```typescript
export type SettingsState = { status: 'idle' | 'success' | 'error'; errors?: Record<string, string[]> }
export async function updateSettings(prevState: SettingsState, formData: FormData): Promise<SettingsState>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Settings page with Server Action</name>
  <files>src/app/actions/settings.ts, src/app/(admin)/admin/settings/page.tsx</files>
  <action>
1. Create src/app/actions/settings.ts:
   - 'use server' directive
   - Zod schema for settings: heroVimeoId (string), aboutText (string, max 5000), contactEmail (string, email), location (string, max 200), instagramUrl (string, max 500), vimeoProfileUrl (string, max 500)
   - Export SettingsState type (same pattern as contact.ts)
   - updateSettings(prevState, formData): verifySession(), validate with Zod, db.siteConfig.upsert({ where: { id: 'singleton' }, create: { ...data }, update: { ...data } }), revalidatePath('/', 'layout'), return { status: 'success' }
   - Handle errors with try/catch, return { status: 'error', errors: { _form: [...] } }

2. Create src/app/(admin)/admin/settings/page.tsx:
   - This needs BOTH a Server Component wrapper and a Client Component form
   - Server Component: await verifySession(), fetch db.siteConfig.findUnique({ where: { id: 'singleton' } }), pass config to client form
   - Create an inline or separate SettingsForm 'use client' component (can be in same file or separate — keep it in the same file since it's simple)
   - SettingsForm uses useActionState(updateSettings, { status: 'idle' })
   - Form fields (single column, simple layout):
     - Hero Vimeo ID: text input (with note: "Enter the Vimeo video ID for the homepage hero")
     - About Text: textarea, 6 rows
     - Contact Email: email input
     - Location: text input
     - Instagram URL: text input
     - Vimeo Profile URL: text input
   - Pre-fill all fields from config prop
   - Show success message when status === 'success' (green text: "Settings saved")
   - Show field errors under each field
   - Save button with pending state
   - Style consistent with admin theme: bg-bg-card form container, max-w-2xl
  </action>
  <verify>
    <automated>cd D:/MEWOskin-website && npx tsc --noEmit 2>&1 | head -20 && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>Settings page loads current config values, saves changes via Server Action, shows success/error feedback. Public site reflects changes after save.</done>
</task>

<task type="auto">
  <name>Task 2: Brand logo management page</name>
  <files>src/app/actions/logos.ts, src/app/(admin)/admin/logos/page.tsx, src/components/admin/logo-grid.tsx</files>
  <action>
1. Create src/app/actions/logos.ts:
   - 'use server' directive
   - uploadLogo(prevState, formData): verifySession(), get file from formData, validate type (png/jpg/webp/svg only) and size (max 2MB), sanitize filename (lowercase, replace non-alphanumeric with hyphens, append timestamp), write to process.cwd()/public/assets/ using writeFile from 'node:fs/promises', revalidatePath('/admin/logos') + revalidatePath('/', 'layout'), return { status: 'success' }
   - deleteLogo(filename): verifySession(), validate filename has no path traversal (reject if contains '/' or '..'), unlink the file from public/assets/, revalidatePath. Return void.
   - Add comment: "NOTE: Files uploaded at runtime on Railway will be lost on redeploy. Re-upload logos after deploy until Railway Volumes are configured."

2. Create src/app/(admin)/admin/logos/page.tsx as Server Component:
   - await verifySession()
   - Scan public/assets/ directory using readdirSync from 'node:fs'
   - Filter to image extensions (.png, .jpg, .jpeg, .webp, .svg)
   - Exclude files starting with 'LOGO_' (site logo, not a brand logo)
   - Pass logo filenames array to LogoGrid client component

3. Create src/components/admin/logo-grid.tsx as 'use client':
   - Accept logos: string[] prop
   - Upload section at top: file input + "Upload Logo" button, useActionState with uploadLogo
   - Show upload status (success/error messages)
   - Grid of current logos (3 cols on md+, 2 on mobile):
     - Each logo: img element with src={`/assets/${filename}`}, filename label below
     - Delete button on each (small red X or trash icon). On click, confirm with window.confirm("Delete this logo?") then call deleteLogo.bind(null, filename) via startTransition
   - Style: bg-bg-card cards with rounded border, p-4, logos rendered with max-h-20 object-contain for uniform display
  </action>
  <verify>
    <automated>cd D:/MEWOskin-website && npx tsc --noEmit 2>&1 | head -20 && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>Logo page shows grid of existing logos from /public/assets/, upload adds new logo, delete removes logo. File validation enforces type and size limits.</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- /admin/settings shows all config fields pre-filled from DB
- Saving settings updates SiteConfig and shows success message
- /admin/logos shows existing brand logos in a grid
- Upload adds a new logo file to /public/assets/
- Delete removes the logo file and updates the grid
</verification>

<success_criteria>
- Settings form covers all SiteConfig fields including new ones (location, Instagram, Vimeo profile)
- Logo management uses filesystem scan (no DB model needed)
- Upload validates file type and size
</success_criteria>

<output>
After completion, create `.planning/phases/05-admin-cms/05-04-SUMMARY.md`
</output>
