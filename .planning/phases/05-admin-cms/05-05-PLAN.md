---
phase: 05-admin-cms
plan: 05
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/app/actions/messages.ts
  - src/app/(admin)/admin/messages/page.tsx
  - src/app/(admin)/admin/messages/[id]/page.tsx
  - src/components/admin/message-list.tsx
autonomous: true
requirements: [ADMIN-12]

must_haves:
  truths:
    - "Contact form submissions appear in the admin inbox"
    - "Unread messages are visually distinct from read messages (bold)"
    - "Clicking a message shows the full detail and marks it as read"
    - "Admin can delete messages"
  artifacts:
    - path: "src/app/actions/messages.ts"
      provides: "markRead and deleteMessage Server Actions"
      exports: ["markRead", "deleteMessage"]
    - path: "src/app/(admin)/admin/messages/page.tsx"
      provides: "Message inbox list page"
      min_lines: 15
    - path: "src/app/(admin)/admin/messages/[id]/page.tsx"
      provides: "Message detail page"
      min_lines: 20
    - path: "src/components/admin/message-list.tsx"
      provides: "Message list client component with delete"
      min_lines: 30
  key_links:
    - from: "src/app/(admin)/admin/messages/[id]/page.tsx"
      to: "src/app/actions/messages.ts"
      via: "markRead on page load"
      pattern: "markRead"
    - from: "src/components/admin/message-list.tsx"
      to: "src/app/actions/messages.ts"
      via: "deleteMessage on button click"
      pattern: "deleteMessage"
---

<objective>
Build the contact message inbox with read/unread tracking, detail view, and delete functionality.

Purpose: Enables the owner to review contact form submissions without database access, with a simple email-inbox-style interface.
Output: Working message inbox at /admin/messages with detail view at /admin/messages/[id].
</objective>

<execution_context>
@C:/Users/Max/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Max/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-admin-cms/05-CONTEXT.md
@.planning/phases/05-admin-cms/05-RESEARCH.md
@.planning/phases/05-admin-cms/05-01-SUMMARY.md

@src/lib/dal.ts
@src/lib/db.ts
@prisma/schema.prisma

<interfaces>
<!-- ContactSubmission model -->
```
id, name, email, company, message, read (boolean), createdAt
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Message Server Actions and inbox list</name>
  <files>src/app/actions/messages.ts, src/app/(admin)/admin/messages/page.tsx, src/components/admin/message-list.tsx</files>
  <action>
1. Create src/app/actions/messages.ts:
   - 'use server' directive
   - markRead(id: string): verifySession(), db.contactSubmission.update({ where: { id }, data: { read: true } }), revalidatePath('/admin/messages'), revalidatePath('/admin'). Returns void.
   - deleteMessage(id: string): verifySession(), db.contactSubmission.delete({ where: { id } }), revalidatePath('/admin/messages'), revalidatePath('/admin'). Returns void.

2. Create src/app/(admin)/admin/messages/page.tsx as Server Component:
   - await verifySession()
   - Fetch messages: db.contactSubmission.findMany({ orderBy: { createdAt: 'desc' } })
   - Count unread: filter or separate count query
   - Render heading "Messages" with unread count badge (e.g., "Messages (3 unread)")
   - Pass messages to MessageList client component

3. Create src/components/admin/message-list.tsx as 'use client':
   - Accept messages prop
   - Render each message as a row:
     - Unread: font-bold text-text-primary, left border accent color
     - Read: font-normal text-text-muted
     - Show: name, email, date (formatted with toLocaleDateString), first 80 chars of message as preview
     - Link to /admin/messages/[id] for full detail (the entire row is clickable)
     - Delete button (small, right side). Use window.confirm before calling deleteMessage via startTransition
   - Empty state: "No messages yet" centered text
   - Style as inbox-like list: bg-bg-card container, divide-y divide-white/10 between items, hover:bg-bg-elevated on each row
  </action>
  <verify>
    <automated>cd D:/MEWOskin-website && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Message inbox shows all submissions sorted by date, unread messages are bold, delete works with confirmation</done>
</task>

<task type="auto">
  <name>Task 2: Message detail page with auto-mark-read</name>
  <files>src/app/(admin)/admin/messages/[id]/page.tsx</files>
  <action>
1. Create src/app/(admin)/admin/messages/[id]/page.tsx as Server Component:
   - await verifySession()
   - Extract id from params (await params for Next.js 16)
   - Fetch message: db.contactSubmission.findUnique({ where: { id } })
   - If not found, call notFound()
   - If message.read is false, call markRead(id) to mark as read (import the Server Action and call it directly — this is server-to-server, no form needed)
   - Render full message detail:
     - Back link to /admin/messages
     - Name (heading)
     - Email (with mailto: link)
     - Company (if not empty)
     - Date (formatted nicely)
     - Full message text (whitespace-pre-wrap for formatting)
   - Delete button at bottom: form with action={deleteMessage.bind(null, id)}, redirects to /admin/messages after delete
   - Style: bg-bg-card container, max-w-2xl, padded, clean typography
  </action>
  <verify>
    <automated>cd D:/MEWOskin-website && npx tsc --noEmit 2>&1 | head -20 && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>Message detail page shows full message, auto-marks as read, email is a mailto link, delete redirects to inbox. Build succeeds.</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- /admin/messages shows all contact submissions with read/unread distinction
- Clicking a message navigates to detail and marks it read
- Returning to inbox shows the message as no longer bold
- Delete removes the message and returns to inbox
- Dashboard unread count reflects actual unread messages
</verification>

<success_criteria>
- Inbox shows messages with clear read/unread visual distinction
- Detail view auto-marks messages as read
- Delete works from both list and detail views
</success_criteria>

<output>
After completion, create `.planning/phases/05-admin-cms/05-05-SUMMARY.md`
</output>
