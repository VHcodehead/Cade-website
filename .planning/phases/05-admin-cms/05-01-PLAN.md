---
phase: 05-admin-cms
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/app/(admin)/layout.tsx
  - src/components/admin/sidebar.tsx
  - src/app/(admin)/admin/page.tsx
autonomous: true
requirements: [ADMIN-01, RESP-03]

must_haves:
  truths:
    - "Admin sidebar navigation is visible on desktop with links to Dashboard, Projects, Settings, Brand Logos, Messages"
    - "Sidebar collapses to hamburger menu on tablet/mobile viewports"
    - "Admin dashboard shows overview with navigation cards and unread message count"
    - "SiteConfig model has location, instagramUrl, vimeoProfileUrl fields"
    - "Project model has thumbnailUrl and previewClipUrl fields"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Updated schema with new fields on SiteConfig and Project"
      contains: "location"
    - path: "src/components/admin/sidebar.tsx"
      provides: "Collapsible sidebar navigation"
      min_lines: 40
    - path: "src/app/(admin)/layout.tsx"
      provides: "Admin layout with sidebar + content area"
      min_lines: 10
    - path: "src/app/(admin)/admin/page.tsx"
      provides: "Dashboard with nav cards and message count"
      min_lines: 20
  key_links:
    - from: "src/app/(admin)/layout.tsx"
      to: "src/components/admin/sidebar.tsx"
      via: "import AdminSidebar"
      pattern: "AdminSidebar"
    - from: "src/app/(admin)/admin/page.tsx"
      to: "src/lib/db.ts"
      via: "db.contactSubmission.count"
      pattern: "contactSubmission\\.count"
---

<objective>
Create the admin shell: run schema migration to add missing fields, build the sidebar navigation layout, and replace the dashboard placeholder with a real overview page.

Purpose: Every subsequent admin plan depends on the sidebar shell being in place and the schema having all required fields.
Output: Working admin layout with sidebar, dashboard with message count badge, and migrated database schema.
</objective>

<execution_context>
@C:/Users/Max/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Max/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-admin-cms/05-CONTEXT.md
@.planning/phases/05-admin-cms/05-RESEARCH.md

@src/app/(admin)/layout.tsx
@src/app/(admin)/admin/page.tsx
@src/lib/dal.ts
@src/lib/db.ts
@src/app/globals.css
@prisma/schema.prisma

<interfaces>
<!-- Existing patterns to follow -->

From src/lib/dal.ts:
```typescript
export const verifySession = cache(async () => {
  // returns { isAuth: true, userId: string } or redirects
});
```

From src/lib/db.ts:
```typescript
// PrismaClient singleton — import { db } from '@/lib/db'
```

From src/app/globals.css (design tokens available):
```css
--color-bg-base, --color-bg-card, --color-bg-elevated
--color-text-primary, --color-text-muted
--color-accent (gold #C8A96E)
--color-border (white/10)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema migration + Admin sidebar layout</name>
  <files>prisma/schema.prisma, src/components/admin/sidebar.tsx, src/app/(admin)/layout.tsx</files>
  <action>
1. Update prisma/schema.prisma:
   - Add to Project model: `thumbnailUrl String @default("")` and `previewClipUrl String @default("")`
   - Add to SiteConfig model: `location String @default("")`, `instagramUrl String @default("")`, `vimeoProfileUrl String @default("")`
   - Run `npx prisma db push` to apply changes (dev workflow, not full migration)

2. Create src/components/admin/sidebar.tsx as a 'use client' component:
   - NAV_LINKS array: Dashboard (/admin), Projects (/admin/projects), Settings (/admin/settings), Brand Logos (/admin/logos), Messages (/admin/messages)
   - Use usePathname() to highlight active link (accent color border-left or bg-bg-elevated)
   - Desktop (md+): fixed sidebar, ~240px wide, bg-bg-card, border-right border-white/10
   - Tablet/mobile (below md): hidden by default, hamburger button fixed top-left z-50, sidebar slides in as overlay with backdrop
   - Use useState for open/close toggle. Include a logout form with the existing logout Server Action at the bottom of the sidebar
   - Include VLACOVISION text logo or "Admin" heading at top of sidebar
   - Style with existing design tokens: bg-bg-card, text-text-primary, text-text-muted, border-white/10, hover:bg-bg-elevated

3. Update src/app/(admin)/layout.tsx:
   - Keep `export const dynamic = 'force-dynamic'`
   - Import AdminSidebar component
   - Render as flex layout: `<div className="min-h-screen bg-bg-base flex">` with AdminSidebar + `<main className="flex-1 overflow-auto p-6 md:p-8">`
  </action>
  <verify>
    <automated>cd D:/MEWOskin-website && npx prisma db push --accept-data-loss 2>&1 | tail -5 && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Schema has new fields, sidebar renders with 5 nav links, layout shows sidebar + content area, hamburger works below md breakpoint</done>
</task>

<task type="auto">
  <name>Task 2: Admin dashboard page with overview cards</name>
  <files>src/app/(admin)/admin/page.tsx</files>
  <action>
Replace the placeholder dashboard page with a real overview:

1. Keep `await verifySession()` as the first line
2. Query `db.contactSubmission.count({ where: { read: false } })` for unread message count
3. Query `db.project.count()` for total projects and `db.project.count({ where: { published: true } })` for published count
4. Render a heading "Dashboard" and a grid of navigation cards (2 cols on md+, 1 col on mobile):
   - Projects card: links to /admin/projects, shows "X projects (Y published)"
   - Messages card: links to /admin/messages, shows "X unread" with a badge if count > 0
   - Settings card: links to /admin/settings, brief description
   - Brand Logos card: links to /admin/logos, brief description
5. Style cards with: bg-bg-card, border border-white/10, rounded-lg, p-6, hover:border-accent transition-colors
6. Use Next.js Link component for card navigation
7. Keep the dark theme aesthetic — minimalist, clean, Linear-style
  </action>
  <verify>
    <automated>cd D:/MEWOskin-website && npx tsc --noEmit 2>&1 | head -20 && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>Dashboard shows 4 navigation cards with real counts from DB, unread message badge visible when count > 0, page builds without errors</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- Visiting /admin shows sidebar with 5 links and dashboard with cards
- Resizing browser below md breakpoint hides sidebar and shows hamburger
- Database schema has thumbnailUrl, previewClipUrl on Project and location, instagramUrl, vimeoProfileUrl on SiteConfig
</verification>

<success_criteria>
- Admin shell is complete: sidebar + layout + dashboard with real data
- Schema migration applied with all fields needed by later plans
- Tablet-responsive sidebar with hamburger collapse
</success_criteria>

<output>
After completion, create `.planning/phases/05-admin-cms/05-01-SUMMARY.md`
</output>
