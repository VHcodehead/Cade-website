---
phase: 06-analytics-and-advanced-admin
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/admin/project-list.tsx
  - src/app/actions/projects.ts
  - package.json
autonomous: true
requirements:
  - ADMIN-07

must_haves:
  truths:
    - "Owner can drag a project row to a new position in the admin project list"
    - "Dropped project row stays in new position immediately (optimistic update)"
    - "New order persists to database and survives page refresh"
    - "Public portfolio grid reflects the new sort order after revalidation"
  artifacts:
    - path: "src/components/admin/project-list.tsx"
      provides: "DragDropContext/Droppable/Draggable wrapping existing project rows with optimistic reorder"
      contains: "DragDropContext"
    - path: "src/app/actions/projects.ts"
      provides: "reorderProjects Server Action with $transaction batch update"
      exports: ["reorderProjects"]
  key_links:
    - from: "src/components/admin/project-list.tsx"
      to: "src/app/actions/projects.ts"
      via: "reorderProjects Server Action call on drag end"
      pattern: "reorderProjects"
    - from: "src/app/actions/projects.ts"
      to: "prisma.project.update"
      via: "$transaction batch sortOrder update"
      pattern: "\\$transaction"
---

<objective>
Add drag-and-drop project reordering to the admin project list using @hello-pangea/dnd.

Purpose: The owner needs to reorder portfolio projects to reflect strategic priorities without editing sortOrder values manually.
Output: Enhanced ProjectList component with vertical drag-and-drop; reorderProjects Server Action persisting new order atomically.
</objective>

<execution_context>
@C:/Users/Max/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Max/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-analytics-and-advanced-admin/06-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/components/admin/project-list.tsx:
```typescript
type Project = {
  id: string
  slug: string
  title: string
  client: string
  published: boolean
  sortOrder: number
  vimeoId: string
  thumbnailUrl: string
  year: number
}

type OptimisticAction =
  | { type: 'toggle'; id: string; published: boolean }
  | { type: 'delete'; id: string }

// Component already uses useOptimistic with optimisticReducer
export function ProjectList({ projects }: { projects: Project[] })
```

From src/app/actions/projects.ts:
```typescript
export async function togglePublished(id: string, published: boolean): Promise<void>
export async function deleteProject(id: string): Promise<void>
// reorderProjects to be added here
```

From prisma/schema.prisma:
```prisma
model Project {
  // ...
  sortOrder Int @default(0)
  // ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @hello-pangea/dnd and add reorderProjects Server Action</name>
  <files>package.json, src/app/actions/projects.ts</files>
  <action>
1. Install @hello-pangea/dnd:
   ```bash
   npm install @hello-pangea/dnd
   ```

2. Add `reorderProjects` Server Action to `src/app/actions/projects.ts`:
   - Accept `items: Array<{ id: string; sortOrder: number }>` parameter
   - Call `verifySession()` first
   - Use `db.$transaction()` passing an array of `db.project.update()` calls — one per item, setting `{ where: { id }, data: { sortOrder } }`
   - After transaction, call `revalidatePath('/admin/projects')` and `revalidatePath('/', 'layout')` to update both admin and public views
   - Export the function

Reference the exact pattern from 06-RESEARCH.md Pattern 2.
  </action>
  <verify>
    <automated>cd D:/MEWOskin-website && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>@hello-pangea/dnd installed. reorderProjects Server Action exists in projects.ts, type-checks clean, uses $transaction for atomic batch update.</done>
</task>

<task type="auto">
  <name>Task 2: Enhance ProjectList with drag-and-drop reorder</name>
  <files>src/components/admin/project-list.tsx</files>
  <action>
Modify the existing `ProjectList` component in `src/components/admin/project-list.tsx` to support vertical drag-and-drop reordering:

1. Add imports: `DragDropContext`, `Droppable`, `Draggable`, `type DropResult` from `@hello-pangea/dnd`. Import `reorderProjects` from `@/app/actions/projects`.

2. Extend the `OptimisticAction` union type with a new variant:
   ```typescript
   | { type: 'reorder'; items: Project[] }
   ```
   Update `optimisticReducer` to handle this case by returning `action.items`.

3. Add a `handleDragEnd(result: DropResult)` function:
   - If `!result.destination` or `result.destination.index === result.source.index`, return early (no-op).
   - Create a reordered copy of `optimisticProjects`: splice out the item at `source.index`, splice it into `destination.index`.
   - Inside `startTransition(async () => { ... })`:
     - Call `dispatchOptimistic({ type: 'reorder', items: reordered })`
     - Call `await reorderProjects(reordered.map((p, i) => ({ id: p.id, sortOrder: i })))` — sends the full array with sequential 0-based sortOrder values

4. Wrap the project list container in `<DragDropContext onDragEnd={handleDragEnd}>` and `<Droppable droppableId="projects">`. The Droppable render prop provides `provided`:
   - The outer `<div className="flex flex-col gap-2">` gets `ref={provided.innerRef}` and `{...provided.droppableProps}`
   - CRITICAL: Render `{provided.placeholder}` as the last child inside the Droppable container

5. Wrap each project row `<div>` in `<Draggable key={project.id} draggableId={project.id} index={index}>`. The Draggable render prop provides `provided`:
   - The project row div gets `ref={provided.innerRef}`, `{...provided.draggableProps}`
   - Add a drag handle element (a 6-dot grip icon or simple ":::" text) at the start of each row that gets `{...provided.dragHandleProps}`. Style it with `cursor-grab text-text-muted hover:text-text-primary`.
   - Adjust the grid template to accommodate the drag handle column: add a `24px` column at the start of the grid definition (both header and rows).

6. Add a "Reorder" column header in the hidden-on-mobile header row to match the new grip column.

7. Keep the header row OUTSIDE the Droppable (it is not a draggable item). Only the project rows are inside the Droppable.

Note: The file already has `'use client'` — no SSR hydration issues. The existing optimistic toggle/delete patterns remain unchanged; drag-and-drop is additive.
  </action>
  <verify>
    <automated>cd D:/MEWOskin-website && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>ProjectList renders with drag handles on each row. Dragging a row to a new position optimistically reorders the list and calls reorderProjects to persist. Existing toggle/delete functionality unchanged.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — zero TypeScript errors
2. `npm run build` — production build succeeds
3. Manual smoke test: visit /admin/projects, drag a project row to a new position, observe it stays in place. Refresh page — order persists. Visit public homepage — portfolio grid reflects new order.
</verification>

<success_criteria>
- @hello-pangea/dnd installed and project-list.tsx imports DragDropContext/Droppable/Draggable
- Each project row has a visible drag handle
- Dragging reorders optimistically (no flash/reload)
- reorderProjects Server Action batch-updates sortOrder via $transaction
- Public portfolio grid order matches admin sort order after revalidation
- All existing project list features (toggle, delete, edit) still work
</success_criteria>

<output>
After completion, create `.planning/phases/06-analytics-and-advanced-admin/06-01-SUMMARY.md`
</output>
