---
phase: 03-public-portfolio
plan: 04
type: execute
wave: 3
depends_on: ["03-02", "03-03"]
files_modified:
  - src/app/(public)/projects/[slug]/page.tsx
  - src/components/portfolio/video-facade.tsx
  - src/app/(public)/page.tsx
  - src/app/layout.tsx
autonomous: true
requirements: [PORT-06, SEO-01, SEO-03]

must_haves:
  truths:
    - "Project detail page shows full Vimeo embed with facade pattern (poster click to play)"
    - "Project metadata (client, services, year) is displayed"
    - "Previous/Next project navigation works at bottom of detail page"
    - "Every page has unique meta title, description, and OG tags"
    - "Project detail pages have project-specific metadata"
    - "Homepage has proper SEO metadata"
    - "Contact form section is wired into homepage"
  artifacts:
    - path: "src/app/(public)/projects/[slug]/page.tsx"
      provides: "Project detail page with video facade, metadata, prev/next nav"
      min_lines: 60
    - path: "src/components/portfolio/video-facade.tsx"
      provides: "Poster image that swaps to Vimeo iframe on click"
      min_lines: 30
  key_links:
    - from: "src/app/(public)/projects/[slug]/page.tsx"
      to: "src/lib/db.ts"
      via: "Prisma query for project by slug"
      pattern: "db\\.project\\.findUnique"
    - from: "src/app/(public)/projects/[slug]/page.tsx"
      to: "src/lib/vimeo.ts"
      via: "getVimeoThumbnail for poster image"
      pattern: "getVimeoThumbnail"
    - from: "src/app/(public)/projects/[slug]/page.tsx"
      to: "src/components/portfolio/video-facade.tsx"
      via: "VideoFacade component"
      pattern: "VideoFacade"
    - from: "src/app/(public)/projects/[slug]/page.tsx"
      to: "src/components/sections/contact-form.tsx"
      via: "CTA at bottom linking to contact"
      pattern: "CTAButton|#contact"
---

<objective>
Build the project detail page with video facade, metadata display, prev/next navigation, and add SEO metadata to all public pages. Wire the contact form into the homepage.

Purpose: Project detail pages are where potential clients evaluate VLACOVISION's work. The video facade ensures fast load, and SEO metadata ensures discoverability. Wiring the contact form completes the homepage.
Output: Fully functional project detail pages with SEO, and complete homepage with contact form integrated.
</objective>

<execution_context>
@C:/Users/Max/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Max/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-public-portfolio/03-CONTEXT.md
@.planning/phases/03-public-portfolio/03-RESEARCH.md
@.planning/phases/03-public-portfolio/03-01-SUMMARY.md
@.planning/phases/03-public-portfolio/03-02-SUMMARY.md
@.planning/phases/03-public-portfolio/03-03-SUMMARY.md

@src/app/globals.css
@src/lib/db.ts
@src/lib/vimeo.ts

<interfaces>
<!-- From Plan 01 -->
From src/lib/vimeo.ts:
export function getVimeoThumbnail(vimeoId: string): Promise<string | null>

From src/components/ui/cta-button.tsx:
export function CTAButton({ variant, href, children })

<!-- From Plan 03 -->
From src/components/sections/contact-form.tsx:
export function ContactForm() — 'use client', section with id="contact"

<!-- Prisma Project model -->
model Project {
  slug: String @unique
  title, client, services, year, description, vimeoId
  published: Boolean, sortOrder: Int
}

<!-- Next.js 16 generateMetadata -->
params is Promise<{ slug: string }> in Next.js 16
export async function generateMetadata({ params }: { params: Promise<{ slug: string }> }): Promise<Metadata>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Video facade component and project detail page</name>
  <files>src/components/portfolio/video-facade.tsx, src/app/(public)/projects/[slug]/page.tsx</files>
  <action>
**1. Create src/components/portfolio/video-facade.tsx ('use client'):**
- Follow RESEARCH.md Pattern 4 closely
- Props: vimeoId (string), thumbnailUrl (string | null), title (string)
- State: isPlaying (boolean, default false)
- When NOT playing: render a button with the thumbnail as background (next/image fill, object-cover, priority, sizes="100vw"). Overlay with a centered play icon (circle with triangle). The circle: w-20 h-20 rounded-full bg-black/50, hover bg-accent transition. Triangle: white SVG play arrow.
- When playing: render Vimeo iframe with `?autoplay=1&title=0&byline=0&portrait=0&dnt=1`, allow="autoplay; fullscreen; picture-in-picture", allowFullScreen
- Container: relative w-full aspect-video bg-black
- Accessibility: button has aria-label="Play {title}"

**2. Create src/app/(public)/projects/[slug]/page.tsx (Server Component):**

**generateMetadata:**
- Export async generateMetadata function
- In Next.js 16, params is a Promise: `const { slug } = await params`
- Fetch project by slug from DB
- Return: title: `${project.title} — VLACOVISION`, description: project.description (truncated to 160 chars), openGraph: { title, description, type: 'video.other', images: [thumbnailUrl] }

**Page component:**
- Fetch project by slug: `db.project.findUnique({ where: { slug } })`
- If not found or not published: call notFound() from 'next/navigation'
- Fetch thumbnail via getVimeoThumbnail(project.vimeoId)
- Fetch prev/next projects for navigation:
  - Previous: `db.project.findFirst({ where: { published: true, sortOrder: { lt: project.sortOrder } }, orderBy: { sortOrder: 'desc' } })`
  - Next: `db.project.findFirst({ where: { published: true, sortOrder: { gt: project.sortOrder } }, orderBy: { sortOrder: 'asc' } })`

**Layout:**
- Full-width VideoFacade at top (no padding)
- Below: max-w-4xl mx-auto px-spacing-4 py-spacing-8
- Project title: h1 font-heading text-3xl sm:text-5xl uppercase tracking-widest mb-4
- Metadata row: flex gap-8, each item labeled:
  - "Client" label (text-xs uppercase text-text-muted) + value (text-sm text-text-primary)
  - "Services" label + value
  - "Year" label + value
- Description: text-lg text-text-muted leading-relaxed mt-spacing-4 (render as paragraphs, split by \n\n if needed)

**Navigation at bottom:**
- Flex justify-between, border-t border-bg-section, pt-spacing-4, mt-spacing-8
- Left: if prev exists, Link to /projects/{prev.slug} with "← {prev.title}" (text-sm text-text-muted hover:text-accent)
- Right: if next exists, Link to /projects/{next.slug} with "{next.title} →"
- Center (or above): Link back to "/#work" — "Back to Portfolio" text

**CTA at bottom:**
- Below nav: centered section py-spacing-8
- "Like what you see? Let's talk." text in font-heading text-xl
- CTAButton "Start a Project" primary variant href="/#contact"
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Project detail page renders at /projects/[slug] with video facade (poster → click → iframe), metadata sidebar, description, prev/next navigation, and CTA. Non-existent slugs return 404. SEO metadata is project-specific.</done>
</task>

<task type="auto">
  <name>Task 2: SEO metadata on homepage and wire contact form</name>
  <files>src/app/(public)/page.tsx, src/app/layout.tsx</files>
  <action>
**1. Add generateMetadata to src/app/(public)/page.tsx:**
- Export generateMetadata returning:
  - title: "VLACOVISION — Premium Video Production"
  - description: "Premium video production for brands that move people. Bay Area and worldwide. Nike, Disney, Lululemon, and more."
  - openGraph: { title, description, type: 'website', siteName: 'VLACOVISION' }

**2. Wire ContactForm into the homepage:**
- Import ContactForm from '@/components/sections/contact-form'
- Replace the contact placeholder section (id="contact" with "Contact coming soon") with the actual `<ContactForm />` component
- The ContactForm component already has its own section wrapper with id="contact"

**3. Update root layout.tsx metadata:**
- Keep as-is for default metadata, but ensure it serves as a good fallback
- The (public)/page.tsx generateMetadata will override for the homepage

**4. Verify all CTA href="#contact" links will scroll to the contact section:**
- The ContactForm section already has id="contact"
- CTAs in footer, after hero, and on project detail pages all link to /#contact
- For same-page CTAs (hero CTA, footer CTA on homepage), the native anchor behavior handles scrolling
- For cross-page CTAs (project detail → /#contact), the # fragment in the URL will scroll on page load

**5. Ensure LCP performance (SEO-03):**
- Hero video iframe is above the fold but does NOT block LCP (iframes are not LCP candidates)
- The actual LCP element will be the hero text or first meaningful paint
- All Vimeo thumbnails in the grid use next/image with lazy loading (default)
- The video facade on detail pages uses priority on the poster image (it IS the LCP element)
- Verify no unnecessary client-side JS blocks initial render
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Homepage has proper OG metadata. Contact form is rendered on the homepage. Project detail pages have project-specific metadata. All CTA links point to #contact and work for both same-page and cross-page navigation.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npm run dev` starts, all pages render without errors
- /projects/[valid-slug] shows video facade, metadata, prev/next nav
- /projects/invalid-slug returns 404
- View page source on homepage: unique title and OG tags present
- View page source on project page: project-specific title and OG tags
- Contact form is visible on homepage, submitting works
- All CTAs scroll to or navigate to contact section
</verification>

<success_criteria>
- Video facade: poster image, play button, click loads Vimeo iframe
- Project detail: title, client, services, year, description displayed
- Prev/Next navigation links to adjacent projects
- CTA at bottom of detail page links to /#contact
- Homepage generateMetadata produces title + description + OG tags
- Project pages generateMetadata produces project-specific title + description + OG image
- Contact form integrated into homepage (no more placeholder)
- Fast initial load: facade defers iframe, thumbnails lazy-loaded
</success_criteria>

<output>
After completion, create `.planning/phases/03-public-portfolio/03-04-SUMMARY.md`
</output>
