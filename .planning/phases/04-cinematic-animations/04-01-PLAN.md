---
phase: 04-cinematic-animations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/animation-config.ts
  - src/components/providers/motion-provider.tsx
  - src/app/(public)/template.tsx
  - src/components/animations/reveal-section.tsx
  - src/components/animations/reveal-grid-item.tsx
autonomous: true
requirements: [ANIM-01, ANIM-05]

must_haves:
  truths:
    - "Navigating between public pages produces a clip-path wipe transition with no blank flash"
    - "When prefers-reduced-motion is enabled, all transform/scale/clipPath animations are skipped and content appears immediately"
    - "Reusable animation wrapper components exist and export correct variants for scroll reveals and staggered grids"
  artifacts:
    - path: "src/lib/animation-config.ts"
      provides: "Centralized easing curves, durations, and stagger constants"
      exports: ["EASING_CINEMATIC", "EASING_SMOOTH", "DURATION", "STAGGER_GRID", "STAGGER_DELAY"]
    - path: "src/components/providers/motion-provider.tsx"
      provides: "MotionConfig reducedMotion='user' wrapping LazyMotion"
      contains: "MotionConfig"
    - path: "src/app/(public)/template.tsx"
      provides: "Enter-only clip-path page transition on every route change"
      contains: "clipPath"
    - path: "src/components/animations/reveal-section.tsx"
      provides: "whileInView fade-up wrapper for section headings"
      exports: ["RevealSection"]
    - path: "src/components/animations/reveal-grid-item.tsx"
      provides: "Staggered grid container and item wrappers"
      exports: ["AnimatedGrid", "AnimatedGridItem", "gridContainerVariants", "gridItemVariants"]
  key_links:
    - from: "src/app/(public)/template.tsx"
      to: "src/lib/animation-config.ts"
      via: "import EASING_CINEMATIC, DURATION"
      pattern: "EASING_CINEMATIC|DURATION"
    - from: "src/components/providers/motion-provider.tsx"
      to: "framer-motion MotionConfig"
      via: "reducedMotion='user'"
      pattern: "reducedMotion"
---

<objective>
Create the animation foundation: shared timing constants, reduced-motion support in the MotionProvider, the clip-path page transition via template.tsx, and reusable scroll-reveal wrapper components.

Purpose: Establish all animation primitives so Plan 02 can integrate them into existing page sections without creating any new animation logic.
Output: 5 new/modified files providing page transitions, reduced-motion support, and reusable animation wrappers.
</objective>

<execution_context>
@C:/Users/Max/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Max/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cinematic-animations/04-CONTEXT.md
@.planning/phases/04-cinematic-animations/04-RESEARCH.md

<interfaces>
<!-- Existing code the executor needs to understand -->

From src/components/providers/motion-provider.tsx:
```typescript
'use client';
import { LazyMotion, domAnimation } from 'framer-motion';

export function MotionProvider({ children }: { children: React.ReactNode }) {
  return (
    <LazyMotion features={domAnimation} strict>
      {children}
    </LazyMotion>
  );
}
```

From src/app/(public)/layout.tsx:
```typescript
export default function PublicLayout({ children }: { children: ReactNode }) {
  return (
    <>
      <link rel="preconnect" href="https://player.vimeo.com" />
      <link rel="preconnect" href="https://i.vimeocdn.com" />
      <Nav />
      <main>{children}</main>
      <Footer />
    </>
  );
}
```

CRITICAL: Project uses LazyMotion strict mode. All animation components MUST use `m` import, NOT `motion`. Using `motion` inside LazyMotion strict throws a runtime error.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Animation config + MotionProvider update + page transition template</name>
  <files>src/lib/animation-config.ts, src/components/providers/motion-provider.tsx, src/app/(public)/template.tsx</files>
  <action>
1. Create `src/lib/animation-config.ts` with centralized animation constants:
   - `EASING_CINEMATIC = [0.76, 0, 0.24, 1] as const` (sharp ease-out for page transitions)
   - `EASING_SMOOTH = [0.25, 0.1, 0.25, 1] as const` (gentle ease-out for reveals)
   - `DURATION = { fast: 0.2, medium: 0.45, cinematic: 0.55 } as const`
   - `STAGGER_GRID = 0.12` (120ms between grid items)
   - `STAGGER_DELAY = 0.05` (50ms initial delay)
   Export all values.

2. Update `src/components/providers/motion-provider.tsx`:
   - Add `MotionConfig` import from `framer-motion`
   - Wrap children with `<MotionConfig reducedMotion="user">` inside `<LazyMotion>`
   - This automatically disables transform/scale/clipPath animations when OS has prefers-reduced-motion: reduce enabled. Opacity still animates (accessibility-compliant middle ground).

3. Create `src/app/(public)/template.tsx` (new file):
   - 'use client' directive
   - Import `m` from `framer-motion` (NOT `motion` — LazyMotion strict)
   - Import `EASING_CINEMATIC` and `DURATION` from `@/lib/animation-config`
   - Default export `Template` component receiving `{ children }`
   - Wrap children in `m.div` with:
     - `initial={{ clipPath: 'inset(0 100% 0 0)' }}` (content hidden from right)
     - `animate={{ clipPath: 'inset(0 0% 0 0)' }}` (horizontal left-to-right wipe reveal)
     - `transition={{ duration: DURATION.medium, ease: EASING_CINEMATIC }}`
   - This is the enter-only page transition. template.tsx re-renders on every route change (unlike layout.tsx which persists). No AnimatePresence needed.
  </action>
  <verify>
    <automated>cd D:/MEWOskin-website && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>animation-config.ts exports all 5 constants. MotionProvider wraps with MotionConfig reducedMotion="user". template.tsx produces a horizontal clip-path wipe on route navigation. TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Reusable scroll-reveal animation wrapper components</name>
  <files>src/components/animations/reveal-section.tsx, src/components/animations/reveal-grid-item.tsx</files>
  <action>
1. Create `src/components/animations/reveal-section.tsx`:
   - 'use client' directive
   - Import `m` from `framer-motion` (NOT `motion`)
   - Import `EASING_SMOOTH`, `DURATION` from `@/lib/animation-config`
   - Define variants object: `hidden: { opacity: 0, y: 18 }`, `visible: { opacity: 1, y: 0 }`
   - Export `RevealSection` component accepting `{ children, className?: string }`
   - Renders `m.div` with:
     - `variants={variants}`
     - `initial="hidden"`
     - `whileInView="visible"`
     - `viewport={{ once: true, margin: '-80px' }}` (triggers 80px into viewport)
     - `transition={{ duration: DURATION.cinematic, ease: EASING_SMOOTH }}`
     - `className={className}`
   - This wraps section headings/content for fade-up-on-scroll reveal.

2. Create `src/components/animations/reveal-grid-item.tsx`:
   - 'use client' directive
   - Import `m` from `framer-motion`
   - Import `EASING_SMOOTH`, `DURATION`, `STAGGER_GRID`, `STAGGER_DELAY` from `@/lib/animation-config`
   - Export `gridContainerVariants`:
     - `hidden: {}`
     - `visible: { transition: { staggerChildren: STAGGER_GRID, delayChildren: STAGGER_DELAY } }`
   - Export `gridItemVariants` ("coming into focus" effect per locked decision):
     - `hidden: { opacity: 0, y: 40, scale: 0.95 }`
     - `visible: { opacity: 1, y: 0, scale: 1, transition: { duration: DURATION.cinematic, ease: EASING_SMOOTH } }`
   - Export `AnimatedGrid` component accepting `{ children, className?: string }`:
     - Renders `m.div` with `variants={gridContainerVariants}`, `initial="hidden"`, `whileInView="visible"`, `viewport={{ once: true, margin: '-50px' }}`, `className`
   - Export `AnimatedGridItem` component accepting `{ children }`:
     - Renders `m.div` with `variants={gridItemVariants}` (inherits whileInView trigger from parent automatically)
   - IMPORTANT: AnimatedGridItem wraps AROUND ProjectCard in PortfolioGrid. It is a separate div above the card, not inside it. This avoids conflicting with ProjectCard's internal IntersectionObserver for lazy-loading thumbnails.
  </action>
  <verify>
    <automated>cd D:/MEWOskin-website && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>RevealSection exported from reveal-section.tsx. AnimatedGrid, AnimatedGridItem, gridContainerVariants, gridItemVariants exported from reveal-grid-item.tsx. Both use `m` (not `motion`). TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- `npm run build` succeeds (full Next.js build validates template.tsx integration)
- All new files use `m` import (not `motion`) — compatible with LazyMotion strict
</verification>

<success_criteria>
- animation-config.ts centralizes all timing constants in one importable module
- MotionProvider includes MotionConfig reducedMotion="user" (ANIM-05 foundation)
- template.tsx produces clip-path wipe on public route navigation (ANIM-01)
- RevealSection and AnimatedGrid/AnimatedGridItem components ready for Plan 02 integration
- TypeScript and build pass cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/04-cinematic-animations/04-01-SUMMARY.md`
</output>
